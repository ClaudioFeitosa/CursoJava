package Secao09Exercicio03;

import java.util.Scanner;

import Aulas.Produto;
import Secao09Exerciciose2.Cadastro_pessoas; 

public class Elevador01 {
	static Scanner teclado = new Scanner(System.in);
	
	static int capacidade, andares, qtd, andar, andarAtual, terreo=0;
	static char chamar, sobe, desce;
	public static void main(String[] args) {
		
		
		System.out.println("informe a capacidade do elavador");
		capacidade = teclado.nextInt();
		System.out.println("o numero de andares");
		andares = teclado.nextInt();
		
		ElevadorClasse elevador = new ElevadorClasse (capacidade, andares,terreo);

		System.out.println("Digite 'S' para subir ou 'D' descer");
			chamar = teclado.nextLine().charAt(0);
			elevador.imprime();	
			
			System.out.println("vc chamou");
				
		if (chamar =='S') {
			System.out.println("informe o andar");
				andar = teclado.nextInt();
				if (andar <0) {
					System.out.println("Andar invalido, por favor digite um valor ente 1 e" + andares);
				}
					
					if (andar > andarAtual) {
						for (int i =0; i<= andar; i++) {
					Sobe(andar);
					
				}
				
			}
		}
		
		
	}
	private static void Sobe(int andar2) {
		
		
	}

}
